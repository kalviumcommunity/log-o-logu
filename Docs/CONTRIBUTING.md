# Contribution Guidelines - Log-o-logu

## 🟢 R — Requirements
- Maintain clean, searchable commits
- Proper feature/fix branching
- Mandatory code review (PR) before merging to `main`

## 🟢 T — Branch Strategy
- `main`: Production-ready code
- `develop`: Ongoing integration
- `feature/*`: New features
- `hotfix/*`: Emergency fixes

## 🟢 C — Commit Conventions
- `feat:` for new features (e.g., `feat: add QR validation module`)
- `fix:` for bug fixes (e.g., `fix: resolve duplicate log creation`)
- `docs:` for documentation updates (e.g., `docs: add LLD documentation`)

## 🟢 R — Code Standards
- Follow official **Dart** formatting rules (`flutter format .`)
- Use meaningful variable and function names (no single-letter names except loops)
- Maintain modular structure (separate UI from business logic)
